[project]
name = "segmentation-models"
version = "0.1.0"
description = ""
authors = [
    {name = "fcas",email = "f3l1nux@gmail.com"}
]
requires-python = ">=3.13"
dependencies = [
    "efficientnet (>=1.1.1,<2.0.0)",
    "image-classifiers (>=1.0.0,<2.0.0)",
    "keras-applications (>=1.0.8,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
